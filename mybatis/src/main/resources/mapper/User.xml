<mappers>
    <mapper namespace="com.example.test.dao.IUserDao">
        <select id="queryUserInfoById" parameterType="java.lang.Long" resultType="com.example.test.dao">
            SELECT *
            FROM user
            where id = #{id}
        </select>
    </mapper>
</mappers>