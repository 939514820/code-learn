<mappers>
    <mapper namespace="com.example.test.dao.IUserDao">
        <select id="queryUserInfoById" parameterType="java.lang.Long" resultType="com.example.test.dao.User">
            SELECT *
            FROM user
            where id = 1
        </select>
    </mapper>

</mappers>